extends layout

include mixins/_downloadForm

block content
    .download
        img.download_img(src=`${finalImage.split('public/')[1]}` draggable="true")
    
    +downloadForm(finalImage.split('public/')[1])