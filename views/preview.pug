extends layout

block content
    h2 Rearrange if you wish
    .preview
        each image in account.imageSources
            img.preview_img(src=`${image}` draggable="true")
    button.btn.stitch(data-handle=`${account.handle}`)
        span Stitch It!
        i.fa.fa-arrow-circle-up