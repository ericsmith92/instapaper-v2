mixin downloadForm(path)

    form#download(action="/download" METHOD="POST")
        input(type="hidden" name="path" value=`${path}`)
        button(type="submit")
            i.fa.fa-arrow-circle-down