mixin searchForm()

    form#search-handle
        label(for="search") Search
        input(type="text" name="search" id="search" placeholder="Search..." autocomplete="off")
        button(type="submit")
            i.fa.fa-search